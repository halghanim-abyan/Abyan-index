# requirements-local.txt — FULL local environment (scrapers + dashboard).
# Use on the machine that runs the daily Playwright scrapers / pipeline:
#   pip install -r requirements-local.txt
# Streamlit Cloud uses the lean requirements.txt instead (no playwright).

# Core / dashboard
pandas==2.2.3
streamlit==1.41.1
plotly==5.24.1
yfinance>=1.2.2

# Local scrapers / backend
playwright==1.49.1
aiosqlite==0.20.0
fastapi>=0.115
uvicorn>=0.30
requests>=2.32
curl_cffi>=0.7
lxml>=5.3

# Excel readers
openpyxl>=3.1
xlrd>=2.0.1

# Shared data layer (Postgres)
SQLAlchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
