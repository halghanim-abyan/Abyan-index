# requirements.txt — used by STREAMLIT CLOUD (read-only dashboard).
# Lean on purpose: NO playwright / curl_cffi / fastapi (heavy, scraper-only)
# so the cloud build is fast and reliable. The full local/scraper env is in
# requirements-local.txt.
#
# Verified against main_app.py's import graph: only these are needed to import
# and run the dashboard (reads from Postgres via db.py).

# RENDERING STACK — pinned to EXACTLY the local dev versions that are known to
# work end-to-end. Letting the cloud resolve "latest" caused two runtime breaks:
#   • streamlit 1.41.1 (old)  → TypeError on st.dataframe(width="stretch")
#   • pyarrow 24.x (too new)  → frontend Arrow-JS "RangeError: index out of range"
#                               (Failed to process a Websocket message)
# These versions coexist locally, so they are mutually compatible by definition.
# If you upgrade Streamlit, re-pin pyarrow/plotly/altair to whatever your dev
# machine resolves alongside it (pip show pyarrow plotly altair).
streamlit==1.55.0
pandas==2.2.3
numpy==2.4.3
pyarrow==23.0.1          # 24+ breaks Streamlit's bundled Arrow-JS reader
altair==6.0.0
plotly==6.6.0
yfinance>=1.2.2
requests>=2.32
lxml>=5.3
openpyxl>=3.1

# Shared data layer (Postgres)
SQLAlchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
